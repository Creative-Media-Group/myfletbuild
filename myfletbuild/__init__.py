import subprocess


def create():
    product = input("Enter name: ")  # can be written in uppercase
    name = product.lower()
    org_domain = "com.example"
    flutter_deps = [""]
    permissions = [""]



def build():
    subprocess.run(["flet"])
